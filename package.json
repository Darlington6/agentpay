{
  "name": "agentpay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "hardhat compile --quiet && mocha --require ./test/setup.js test/**/*.test.js --timeout 60000",
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network base-sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.3",
    "@types/mocha": "^10.0.10",
    "chai": "^6.2.2",
    "dotenv": "^17.3.1",
    "ethers": "^6.16.0",
    "hardhat": "^3.1.12",
    "mocha": "^11.7.5"
  }
}
